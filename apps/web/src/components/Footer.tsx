import type React from "react";

export function Footer(): React.ReactElement {
  return (
    <footer className="border-t border-border bg-surface py-4 text-center text-sm text-text-secondary">
      <p>Sistema de optimización de compra minorista mediante datos abiertos de precios</p>
    </footer>
  );
}
